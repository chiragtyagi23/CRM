# Demo APIs — JSON in `public/demo-api/`. Override with absolute URLs if needed.

VITE_NAV_API_URL=/demo-api/nav.json

# CRM Backend API (Express)
# Production:
VITE_CRM_API_URL=""
# Local dev:
# VITE_CRM_API_URL=http://localhost:4000

# Campaign microsite (eye icon / preview links). Unset → http://localhost:3000
VITE_CAMPAIGN_SITE_URL=""
